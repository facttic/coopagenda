<h1>Listing Proposals</h1>

<table>
  <thead>
    <tr>
      <th>Title</th>
      <th>Details</th>
      <th>Hosted by</th>
      <th>Hosting coop</th>
      <th>Accepted</th>

      <th></th>
    </tr>
  </thead>
  <tbody>
<%= for proposal <- @proposals do %>
    <tr>
      <td><%= proposal.title %></td>
      <td><%= proposal.details %></td>
      <td><%= proposal.hosted_by %></td>
      <td><%= proposal.hosting_coop %></td>
      <td><%= proposal.accepted %></td>

      <td>
        <span><%= link "Show", to: Routes.proposal_path(@conn, :show, proposal) %></span>
        <span><%= link "Edit", to: Routes.proposal_path(@conn, :edit, proposal) %></span>
        <span><%= link "Delete", to: Routes.proposal_path(@conn, :delete, proposal), method: :delete, data: [confirm: "Are you sure?"] %></span>
      </td>
    </tr>
<% end %>
  </tbody>
</table>

<span><%= link "New Proposal", to: Routes.proposal_path(@conn, :new) %></span>
